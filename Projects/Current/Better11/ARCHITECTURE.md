# Driver Manager - Architecture Diagram
## Better11 - System Enhancement Suite

```
┌─────────────────────────────────────────────────────────────────────────────────┐
│                              USER INTERFACE LAYER                                │
├─────────────────────────────────────────────────────────────────────────────────┤
│                                                                                   │
│  ┌─────────────────────────────────────────────────────────────────────────┐   │
│  │                     DriverManagerDashboard.tsx                          │   │
│  │  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐                 │   │
│  │  │  All Drivers │  │   Updates    │  │   Hardware   │  [Tab Navigation]│   │
│  │  └──────────────┘  └──────────────┘  └──────────────┘                 │   │
│  │                                                                          │   │
│  │  ┌─────────────────────────────────────────────────────────────────┐  │   │
│  │  │  📊 Stats: 150 drivers | 10 updates | 2 downloading | 1 installing│  │   │
│  │  └─────────────────────────────────────────────────────────────────┘  │   │
│  └─────────────────────────────────────────────────────────────────────────┘   │
│                                                                                   │
│  ┌──────────────┐  ┌────────────────────────────────────────────────────────┐ │
│  │  Filters     │  │             DriverList.tsx                            │ │
│  │              │  │  ┌──────────────────────────────────────────────┐    │ │
│  │  □ Display   │  │  │  DriverCard - NVIDIA GeForce RTX 4090       │    │ │
│  │  □ Network   │  │  │  ✓ Installed | v545.84 | Update Available   │    │ │
│  │  □ Audio     │  │  │  [Update] [Backup] [•••]                     │    │ │
│  │              │  │  └──────────────────────────────────────────────┘    │ │
│  │  Status:     │  │  ┌──────────────────────────────────────────────┐    │ │
│  │  □ Updated   │  │  │  DriverCard - Realtek Audio                 │    │ │
│  │  □ Outdated  │  │  │  ✓ Installed | v6.0.9441.1                  │    │ │
│  │              │  │  │  [Backup] [•••]                              │    │ │
│  │  [Search]    │  │  └──────────────────────────────────────────────┘    │ │
│  └──────────────┘  └────────────────────────────────────────────────────────┘ │
│                                                                                   │
└───────────────────────────────────────────────────────────────────────────────┬─┘
                                        │
                                        │ Uses
                                        ▼
┌─────────────────────────────────────────────────────────────────────────────────┐
│                               REACT HOOKS LAYER                                  │
├─────────────────────────────────────────────────────────────────────────────────┤
│                                                                                   │
│  useDrivers()          useDriverUpdates()       useDownloads()                  │
│  useDriver(id)         useBatchUpdate()         useInstallations()              │
│  useHardware()         useDriverBackups()       useDownloadProgress(id)         │
│  useDevicesByClass()   useRestorePoints()       useInstallProgress(id)          │
│  useDriverSelection()  useDriverFilters()       useDriverStats()                │
│  useDriverSort()       useDriverSettings()      useDriverEvents()               │
│                        useScanStatus()          useDriverManagerInit()          │
│                                                                                   │
└───────────────────────────────────────────────────────────────────────────────┬─┘
                                        │
                                        │ Read/Write
                                        ▼
┌─────────────────────────────────────────────────────────────────────────────────┐
│                            STATE MANAGEMENT LAYER                                │
├─────────────────────────────────────────────────────────────────────────────────┤
│                                                                                   │
│  ┌───────────────────────────────────────────────────────────────────────────┐ │
│  │                    driverManagerStore (Zustand + Immer)                   │ │
│  ├───────────────────────────────────────────────────────────────────────────┤ │
│  │                                                                            │ │
│  │  STATE:                           ACTIONS:                                │ │
│  │  ├─ hardware                      ├─ scanHardware()                      │ │
│  │  ├─ drivers: Map<id, DriverInfo>  ├─ scanDrivers()                       │ │
│  │  ├─ updates: Map<id, Update>      ├─ checkForUpdates()                   │ │
│  │  ├─ downloadQueue: Map            ├─ startDownload()                     │ │
│  │  ├─ installQueue: Map             ├─ installDriver()                     │ │
│  │  ├─ batchOperations: Map          ├─ startBatchOperation()               │ │
│  │  ├─ backups: Map                  ├─ createBackup()                      │ │
│  │  ├─ restorePoints: []             ├─ restoreBackup()                     │ │
│  │  ├─ selectedDrivers: Set          ├─ selectDriver()                      │ │
│  │  ├─ filters: {...}                ├─ setFilters()                        │ │
│  │  ├─ sortOptions: {...}            ├─ setSortOptions()                    │ │
│  │  ├─ settings: {...}               ├─ updateSettings()                    │ │
│  │  └─ events: []                    └─ addEvent()                          │ │
│  │                                                                            │ │
│  │  COMPUTED:                                                                │ │
│  │  ├─ getFilteredDrivers()      ├─ getDriversNeedingUpdate()              │ │
│  │  ├─ getDriverById(id)         ├─ getDriversByClass(class)               │ │
│  │  └─ getUpdateById(id)         └─ getDriversByStatus(status)             │ │
│  │                                                                            │ │
│  └───────────────────────────────────────────────────────────────────────────┘ │
│                                                                                   │
│  Middleware: Persist (settings), DevTools                                       │
│                                                                                   │
└───────────────────────────────────────────────────────────────────────────────┬─┘
                                        │
                                        │ Calls
                                        ▼
┌─────────────────────────────────────────────────────────────────────────────────┐
│                               SERVICE LAYER                                      │
├─────────────────────────────────────────────────────────────────────────────────┤
│                                                                                   │
│  ┌──────────────────────────────────────────────────────────────────────────┐  │
│  │                     DriverManagerService                                  │  │
│  │                                                                            │  │
│  │  ┌─────────────────────────┐  ┌─────────────────────────┐               │  │
│  │  │ HardwareDetectionService│  │ DriverDetectionService  │               │  │
│  │  │                         │  │                         │               │  │
│  │  │ • scanHardware()        │  │ • scanDrivers()         │               │  │
│  │  │ • getDeviceInfo()       │  │ • getDriverInfo()       │               │  │
│  │  │ • checkDeviceStatus()   │  │ • checkForUpdates()     │               │  │
│  │  └─────────────────────────┘  │ • searchWindowsUpdate() │               │  │
│  │                                 └─────────────────────────┘               │  │
│  │                                                                            │  │
│  │  ┌─────────────────────────┐  ┌─────────────────────────┐               │  │
│  │  │ DriverDownloadService   │  │ DriverInstallationService│              │  │
│  │  │                         │  │                         │               │  │
│  │  │ • downloadDriver()      │  │ • installDriver()       │               │  │
│  │  │ • cancelDownload()      │  │ • updateDriver()        │               │  │
│  │  │ • verifyPackage()       │  │ • uninstallDriver()     │               │  │
│  │  └─────────────────────────┘  │ • rollbackDriver()      │               │  │
│  │                                 │ • batchOperation()      │               │  │
│  │                                 └─────────────────────────┘               │  │
│  │                                                                            │  │
│  │  ┌─────────────────────────┐  ┌─────────────────────────┐               │  │
│  │  │ DriverBackupService     │  │ OEMDriverService        │               │  │
│  │  │                         │  │                         │               │  │
│  │  │ • backupDriver()        │  │ • searchOEMDrivers()    │               │  │
│  │  │ • backupAllDrivers()    │  │ • getNvidiaDrivers()    │               │  │
│  │  │ • restoreDriver()       │  │ • getAMDDrivers()       │               │  │
│  │  │ • listBackups()         │  │ • getIntelDrivers()     │               │  │
│  │  │ • createRestorePoint()  │  │                         │               │  │
│  │  └─────────────────────────┘  └─────────────────────────┘               │  │
│  │                                                                            │  │
│  └──────────────────────────────────────────────────────────────────────────┘  │
│                                                                                   │
└───────────────────────────────────────────────────────────────────────────────┬─┘
                                        │
                                        │ Invokes
                                        ▼
┌─────────────────────────────────────────────────────────────────────────────────┐
│                           TAURI/RUST BACKEND LAYER                               │
├─────────────────────────────────────────────────────────────────────────────────┤
│                                                                                   │
│  Tauri Commands (25+ commands to implement):                                    │
│                                                                                   │
│  ┌──────────────────────┐  ┌──────────────────────┐  ┌────────────────────┐   │
│  │  Hardware Detection  │  │  Driver Detection    │  │  Download          │   │
│  │                      │  │                      │  │                    │   │
│  │  • scan_hardware     │  │  • scan_drivers      │  │  • download_driver │   │
│  │  • get_device_info   │  │  • get_driver_info   │  │  • cancel_download │   │
│  │  • check_device_     │  │  • check_driver_     │  │  • verify_package  │   │
│  │    status            │  │    updates           │  │                    │   │
│  └──────────────────────┘  └──────────────────────┘  └────────────────────┘   │
│                                                                                   │
│  ┌──────────────────────┐  ┌──────────────────────┐  ┌────────────────────┐   │
│  │  Installation        │  │  Backup & Restore    │  │  OEM Integration   │   │
│  │                      │  │                      │  │                    │   │
│  │  • install_driver    │  │  • backup_driver     │  │  • search_oem_     │   │
│  │  • update_driver     │  │  • backup_all_       │  │    drivers         │   │
│  │  • uninstall_driver  │  │    drivers           │  │  • get_oem_drivers │   │
│  │  • rollback_driver   │  │  • restore_driver    │  │                    │   │
│  │  • batch_operation   │  │  • list_backups      │  │                    │   │
│  │                      │  │  • create_restore_   │  │                    │   │
│  │                      │  │    point             │  │                    │   │
│  └──────────────────────┘  └──────────────────────┘  └────────────────────┘   │
│                                                                                   │
└───────────────────────────────────────────────────────────────────────────────┬─┘
                                        │
                                        │ Calls
                                        ▼
┌─────────────────────────────────────────────────────────────────────────────────┐
│                              WINDOWS API LAYER                                   │
├─────────────────────────────────────────────────────────────────────────────────┤
│                                                                                   │
│  Windows Management Instrumentation (WMI)                                       │
│  ├─ Win32_PnPEntity           (Device enumeration)                              │
│  ├─ Win32_PnPSignedDriver     (Driver information)                              │
│  ├─ Win32_VideoController     (GPU information)                                 │
│  └─ Win32_NetworkAdapter      (Network devices)                                 │
│                                                                                   │
│  Setup API                                                                       │
│  ├─ SetupDiGetClassDevs       (Device enumeration)                              │
│  ├─ SetupDiGetDeviceRegistryProperty  (Device properties)                       │
│  └─ SetupDiEnumDriverInfo     (Driver enumeration)                              │
│                                                                                   │
│  Plug and Play (PnP) API                                                        │
│  ├─ UpdateDriverForPlugAndPlayDevices  (Driver installation)                   │
│  ├─ DiInstallDriver           (Advanced installation)                           │
│  └─ CM_Get_DevNode_Status     (Device status)                                   │
│                                                                                   │
│  Windows Update API                                                              │
│  ├─ IUpdateSession            (Update sessions)                                 │
│  ├─ IUpdateSearcher           (Update search)                                   │
│  └─ IUpdateDownloader         (Update downloads)                                │
│                                                                                   │
│  Registry API                                                                    │
│  └─ (Driver information, backup metadata)                                       │
│                                                                                   │
└─────────────────────────────────────────────────────────────────────────────────┘
```

---

## Data Flow Examples

### **1. Scanning for Drivers**

```
User clicks "Scan" button
        │
        ▼
DriverManagerDashboard calls useScanStatus().performFullScan()
        │
        ▼
performFullScan() → scanDrivers() + checkForUpdates()
        │
        ▼
Store action scanDrivers() is triggered
        │
        ▼
Store calls DriverDetectionService.scanDrivers()
        │
        ▼
Service invokes Tauri command 'scan_drivers'
        │
        ▼
Rust backend calls Windows API (WMI + SetupAPI)
        │
        ▼
Backend returns DriverScanResult to frontend
        │
        ▼
Service returns data to store
        │
        ▼
Store updates state.drivers, state.lastScanResult
        │
        ▼
Store adds event to state.events
        │
        ▼
Components re-render with new data
        │
        ▼
User sees updated driver list
```

### **2. Installing a Driver Update**

```
User clicks "Update" on a driver card
        │
        ▼
DriverCard calls useDriver().install()
        │
        ▼
useDriver() hooks into store.installDriver(driverId)
        │
        ▼
Store action installDriver() executes:
  ├─ Creates backup (if enabled)
  ├─ Initializes progress in installQueue
  └─ Calls DriverInstallationService.installDriver()
        │
        ▼
Service invokes Tauri command 'install_driver'
        │
        ▼
Rust backend:
  ├─ Downloads driver if needed
  ├─ Verifies package integrity
  ├─ Creates system restore point
  ├─ Calls Windows PnP API
  └─ Sends progress updates via events
        │
        ▼
Progress updates flow back to store
        │
        ▼
Store updates installQueue with progress
        │
        ▼
useInstallProgress() hook detects changes
        │
        ▼
DriverCard renders progress bar
        │
        ▼
Installation completes
        │
        ▼
Store updates driver status to 'installed'
        │
        ▼
Store removes from installQueue
        │
        ▼
Store adds completion event
        │
        ▼
UI shows success notification
```

### **3. Filtering Drivers**

```
User types in search box or selects filter
        │
        ▼
DriverFilters calls useDriverFilters().setFilters()
        │
        ▼
Store action setFilters() updates state.filters
        │
        ▼
DriverList calls useDrivers().drivers
        │
        ▼
useDrivers() hooks into store.getFilteredDrivers()
        │
        ▼
getFilteredDrivers() computed property:
  ├─ Reads state.drivers
  ├─ Applies state.filters (class, status, search, etc.)
  ├─ Applies state.sortOptions
  └─ Returns filtered & sorted array
        │
        ▼
DriverList re-renders with filtered drivers
        │
        ▼
DriverCard components render for visible drivers only
```

---

## Component Hierarchy

```
App
└── Better11 Main
    └── Module Router
        └── Driver Manager Route
            └── DriverManagerDashboard
                ├── Header
                │   ├── Stats Cards (5)
                │   └── ScanButton
                │
                ├── Tab Navigation
                │   ├── "All Drivers" Tab
                │   ├── "Updates" Tab
                │   └── "Hardware" Tab
                │
                ├── Sidebar (Filters)
                │   └── DriverFilters
                │       ├── Device Class Filters
                │       ├── Status Filters
                │       ├── Manufacturer Filter
                │       └── Search Input
                │
                ├── Main Content Area
                │   ├── [All Drivers View]
                │   │   └── DriverList
                │   │       └── DriverCard (repeated)
                │   │           ├── Checkbox
                │   │           ├── DeviceIcon
                │   │           ├── Driver Info
                │   │           ├── Progress Bars
                │   │           ├── Status Badge
                │   │           └── Action Buttons
                │   │
                │   ├── [Updates View]
                │   │   └── DriverUpdatePanel
                │   │       ├── Critical Updates
                │   │       ├── Recommended Updates
                │   │       └── Optional Updates
                │   │
                │   └── [Hardware View]
                │       └── HardwareOverview
                │           ├── CPU Info
                │           ├── GPU List
                │           ├── Network Devices
                │           └── Other Devices
                │
                └── Right Sidebar (Activity)
                    └── DriverStats
                        ├── Recent Events
                        ├── Download Queue
                        ├── Install Queue
                        └── Statistics
```

---

## State Flow Diagram

```
┌────────────────────────────────────────────────────────────────┐
│                                                                  │
│  USER ACTION → HOOK → STORE ACTION → SERVICE → TAURI → WINDOWS │
│                                                                  │
│                ←──────── RESPONSE FLOWS BACKWARDS ←─────────    │
│                                                                  │
└────────────────────────────────────────────────────────────────┘

Detailed Flow:

1. User Interaction
   └─> React Event Handler
       └─> Custom Hook Method
           └─> Store Action
               └─> Service Method
                   └─> Tauri Command (invoke)
                       └─> Rust Handler
                           └─> Windows API
                               
2. Response Path (Reverse)
   Windows API Result
       └─> Rust processes and wraps in ApiResponse
           └─> Tauri returns to frontend
               └─> Service receives and validates
                   └─> Store updates state
                       └─> Store triggers re-render
                           └─> Hooks detect changes
                               └─> Components update
                                   └─> UI reflects new state
```

---

## Integration Points

```
┌─────────────────────────────────────────────────────────┐
│  Better11 - System Enhancement Suite                    │
├─────────────────────────────────────────────────────────┤
│                                                          │
│  ┌────────────────┐  ┌────────────────┐  ┌──────────┐ │
│  │ App Installer  │  │ Driver Manager │  │ Registry │ │
│  │     Module     │◄─┤   Module       │─►│  Editor  │ │
│  └────────────────┘  └────────────────┘  └──────────┘ │
│         │                    │                  │       │
│         └────────────────────┼──────────────────┘       │
│                              │                          │
│                    ┌─────────▼─────────┐               │
│                    │  Shared Services  │               │
│                    │  - Notifications  │               │
│                    │  - Settings       │               │
│                    │  - Logging        │               │
│                    │  - Analytics      │               │
│                    └───────────────────┘               │
│                                                          │
└─────────────────────────────────────────────────────────┘
```

Potential Integration Points:
1. **App Installer** - Can trigger driver updates after app installation
2. **Registry Editor** - Can modify driver registry keys
3. **System Tweaks** - Can optimize driver-related system settings
4. **Shared Notifications** - Unified notification system
5. **Shared Settings** - Global Better11 settings
6. **Shared Logging** - Centralized event logging

---

**Visual Architecture Complete** ✅
